<%= form_for(@spending_rate) do |f| %>
  <% if @spending_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spending_rate.errors.count, "error") %> prohibited this spending_rate from being saved:</h2>

      <ul>
      <% @spending_rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>%
  </div>
  <div class="field">
    <%= f.label :spending_category_id %><br />
	<%= select("spending_rate", "spending_category_id", SpendingCategory.find(:all).collect{|c| [c.name, c.id]} ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
